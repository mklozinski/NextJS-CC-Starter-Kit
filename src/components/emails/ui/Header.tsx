import * as React from "react";
import { Section } from "@react-email/components";

export default function EmailHeader() {

  
  return (
    <Section className="mt-[16px] px-[5px]">
      
    </Section>
  );
}